[project]
name = "csv-merging"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "moto>=5.1.16",
    "pandas>=2.3.3",
    "pytest>=9.0.1",
]
